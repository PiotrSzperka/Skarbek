# Copy this file to deploy/proxmox/.env.local when you want to run the stack locally or in a private environment.
# Keep the `.env.local` file out of git (see project .gitignore) so secrets never leak.

# Backend overrides -----------------------------------------------------------
# These are typically the same values as deploy/proxmox/.env.example but you can reconfigure ports, DB or admin credentials for local testing.
#CLEAN copies of the existing values are optional.

# Gmail API credentials for sending temporary passwords
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=
GMAIL_SENDER_EMAIL=

# Optional overrides (if you need custom token URI or scopes during testing)
#GMAIL_TOKEN_URI=https://oauth2.googleapis.com/token
#GMAIL_SCOPES=https://www.googleapis.com/auth/gmail.send

# Hint: set these so pgAdmin can access the database UI (optional)
PGADMIN_EMAIL=admin@skarbek.local
PGADMIN_PASSWORD=changeme
